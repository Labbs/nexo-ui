import { default as React } from 'react';
import { OpenBlockEditor } from '@labbs/openblock-core';
/**
 * Props for LinkPopover component.
 */
export interface LinkPopoverProps {
    /**
     * The OpenBlockEditor instance.
     */
    editor: OpenBlockEditor;
    /**
     * Current URL if text already has a link, null otherwise.
     */
    currentUrl: string | null;
    /**
     * Callback when popover is closed.
     */
    onClose: () => void;
    /**
     * Position for the popover.
     */
    position: {
        left: number;
        top: number;
    };
    /**
     * Additional class name.
     */
    className?: string;
    /**
     * Reference to the trigger element (e.g., link button).
     * Clicks on this element won't close the popover.
     */
    triggerRef?: React.RefObject<HTMLElement>;
}
/**
 * LinkPopover component.
 *
 * A compact floating popover for managing links on selected text.
 */
export declare function LinkPopover({ editor, currentUrl, onClose, position, className, triggerRef, }: LinkPopoverProps): React.ReactElement;
//# sourceMappingURL=LinkPopover.d.ts.map