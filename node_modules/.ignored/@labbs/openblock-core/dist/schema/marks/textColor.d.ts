import { MarkSpec } from 'prosemirror-model';
/**
 * Text color mark spec.
 *
 * Renders as `<span style="color: ...">` in HTML.
 *
 * @example
 * ```typescript
 * // In block content:
 * {
 *   type: 'text',
 *   text: 'Red text',
 *   styles: {
 *     textColor: '#ff0000'
 *   }
 * }
 * ```
 */
export declare const textColorMark: MarkSpec;
//# sourceMappingURL=textColor.d.ts.map