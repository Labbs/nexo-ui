import { MarkSpec } from 'prosemirror-model';
/**
 * Bold mark spec.
 *
 * Renders as `<strong>` in HTML. Triggered by Cmd/Ctrl+B.
 *
 * @example
 * ```typescript
 * // In block content:
 * { type: 'text', text: 'Bold text', styles: { bold: true } }
 * ```
 */
export declare const boldMark: MarkSpec;
//# sourceMappingURL=bold.d.ts.map