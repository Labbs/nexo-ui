import { EditorState, Transaction } from 'prosemirror-state';
import { Node as PMNode } from 'prosemirror-model';
/**
 * Command function type
 */
type Command = (state: EditorState, dispatch?: (tr: Transaction) => void) => boolean;
/**
 * Find the table, row, and cell at the current selection.
 * Returns null if not inside a table.
 */
export interface TableContext {
    table: PMNode;
    tablePos: number;
    row: PMNode;
    rowPos: number;
    rowIndex: number;
    cell: PMNode;
    cellPos: number;
    cellIndex: number;
    colCount: number;
    rowCount: number;
}
export declare function findTableContext(state: EditorState): TableContext | null;
/**
 * Add a row after the current row.
 */
export declare const addRowAfter: Command;
/**
 * Add a row before the current row.
 */
export declare const addRowBefore: Command;
/**
 * Delete the current row.
 */
export declare const deleteRow: Command;
/**
 * Add a column after the current column.
 * Uses replaceWith to rebuild each row with the new cell.
 */
export declare const addColumnAfter: Command;
/**
 * Add a column before the current column.
 * Uses replaceWith to rebuild each row with the new cell.
 */
export declare const addColumnBefore: Command;
/**
 * Delete the current column.
 * Uses replaceWith to rebuild each row without the cell.
 */
export declare const deleteColumn: Command;
/**
 * Delete the entire table.
 */
export declare const deleteTable: Command;
/**
 * Move to the next cell (or create a new row if at the end).
 * Typically bound to Tab.
 */
export declare const goToNextCell: Command;
/**
 * Move to the previous cell.
 * Typically bound to Shift+Tab.
 */
export declare const goToPreviousCell: Command;
/**
 * Check if the selection is inside a table.
 */
export declare function isInTable(state: EditorState): boolean;
/**
 * Get information about the current table context.
 */
export declare function getTableInfo(state: EditorState): {
    rowIndex: number;
    cellIndex: number;
    rowCount: number;
    colCount: number;
} | null;
/**
 * Add a row at a specific index.
 */
export declare function addRowAtIndex(state: EditorState, dispatch: ((tr: Transaction) => void) | undefined, tablePos: number, rowIndex: number): boolean;
/**
 * Add a column at a specific index.
 */
export declare function addColumnAtIndex(state: EditorState, dispatch: ((tr: Transaction) => void) | undefined, tablePos: number, colIndex: number): boolean;
/**
 * Delete a row at a specific index.
 */
export declare function deleteRowAtIndex(state: EditorState, dispatch: ((tr: Transaction) => void) | undefined, tablePos: number, rowIndex: number): boolean;
/**
 * Delete a column at a specific index.
 */
export declare function deleteColumnAtIndex(state: EditorState, dispatch: ((tr: Transaction) => void) | undefined, tablePos: number, colIndex: number): boolean;
export {};
//# sourceMappingURL=tableCommands.d.ts.map