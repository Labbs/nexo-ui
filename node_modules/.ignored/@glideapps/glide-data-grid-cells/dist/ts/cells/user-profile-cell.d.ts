import { CustomCell } from "@glideapps/glide-data-grid";
import { CustomCellRenderer } from "../types";
interface UserProfileCellProps {
    readonly kind: "user-profile-cell";
    readonly image: string;
    readonly initial: string;
    readonly tint: string;
    readonly name?: string;
}
export declare type UserProfileCell = CustomCell<UserProfileCellProps>;
declare const renderer: CustomCellRenderer<UserProfileCell>;
export default renderer;
