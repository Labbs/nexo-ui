import { CustomCell } from "@glideapps/glide-data-grid";
import { CustomCellRenderer } from "../types";
interface SparklineCellProps {
    readonly kind: "sparkline-cell";
    readonly graphKind?: "line" | "bar";
    readonly values: readonly number[];
    readonly displayValues?: readonly string[];
    readonly yAxis: readonly [number, number];
    readonly color?: string;
}
export declare type SparklineCell = CustomCell<SparklineCellProps>;
declare const renderer: CustomCellRenderer<SparklineCell>;
export default renderer;
